import React from "react";

export type IndexButtonsProps = {
	linkToTimeline: string;
	onRegisterButtonClicked?: React.MouseEventHandler;
	onLoginButtonClicked?: React.MouseEventHandler;
};

export const IndexButtons: React.VFC<IndexButtonsProps> = (p) => {
	return (
		<>
		</>
	);
};
